const PlaygroundPage = () => {
  return (
    <div className="flex min-h-full items-center justify-center">
      {"<<-- Click for select color on the left panel"}
    </div>
  );
};

export default PlaygroundPage;
